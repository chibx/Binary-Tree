{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "lldb",
			"request": "launch",
			"name": "LLDB: debug the active target",
			"program": "${workspaceFolder}/main",
			"args": [],
			"cwd": "${workspaceFolder}"
		}
	]
}
